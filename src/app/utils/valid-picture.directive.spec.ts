import { ValidPictureDirective } from './valid-picture.directive';

describe('ValidPictureDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidPictureDirective();
    expect(directive).toBeTruthy();
  });
});
